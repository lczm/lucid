#pragma once

// If planning to add more default assets add them to here, the defaultAssets vector in uiSystem.h and the drag and drop portion in UiSystem::DrawScene
enum class DefaultAssetsType
{
  Cube,
  Sphere,
  Line
};
